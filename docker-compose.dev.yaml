services:
  doggopaste_db:
    image: postgres:17
    container_name: doggopaste_db
    restart: unless-stopped
    environment:
      POSTGRES_USER: doggo
      POSTGRES_PASSWORD: changeme
      POSTGRES_DB: doggopaste
    ports:
      - 5432:5432
    volumes:
      - doggopaste_db_data:/var/lib/postgresql/data

volumes:
  doggopaste_db_data: